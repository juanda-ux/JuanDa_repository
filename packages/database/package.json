{
  "name": "@websmith/database",
  "version": "0.1.0",
  "scripts": {
    "prisma": "prisma",
    "migrate": "prisma migrate deploy",
    "generate": "prisma generate",
    "seed": "ts-node src/seed.ts",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "test": "vitest run"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1",
    "argon2": "^0.32.0",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "prisma": "^5.9.1",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
